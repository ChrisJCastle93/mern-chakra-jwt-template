import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    brand: {
      // 100: "#111",
      // 200: "#111",
      // 300: "#111",
      // 400: "#fff",
      500: "#1d3557", // Primary Brand
      600: "#457b9d", // Hover
      // 700: "#111",
      // 800: "#111",
      // 900: "#111",
    },
  },
});

export default theme;
